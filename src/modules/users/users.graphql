type User {
  id: ID!
  firstName: String
  lastName: String
  password: String
  email: String!
}

input UserInput {
  firstName: String
  lastName: String
  password: String
  email: String!
}

type Mutation {
  register(body: UserInput): User
}